#include<iostream>
using namespace std;

int main(){

    int length,breadth,area;
    cout<<"length : ";
    cin>>length;
    cout<<"breadth : ";
    cin>>breadth;

    area=length * breadth;
    
    cout<<"area of the rectangle is "<<area<<endl;

    return 0;
}